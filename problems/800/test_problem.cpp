#include <bits/stdc++.h>

using namespace std;

// Fast I/O
void fast_io() {
    ios::sync_with_stdio(0);
    cin.tie(0);
}

// Common type aliases
typedef long long ll;
typedef vector<int> vi;
typedef pair<int, int> pii;

void solve() {
    string s;
    if (cin >> s) {
        cout << "Read from input: " << s << endl;
    }
}

int main() {
    fast_io();
    int t = 1;
    // cin >> t; // Uncomment if multiple test cases
    while (t--) {
        solve();
    }
    return 0;
}
